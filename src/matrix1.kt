fun main() {
    val n = 18
    val m = 24
    val mat = mutableListOf<Int>()
        for (j in 0 ..m) {
    for (i in 0 .. n) {
       mat.add(i+j)}
            println(mat)
    mat.clear()}
        }
    //println()

//    val string = mutableListOf<Int>()
//    val matrix = mutableListOf<Int>()
//    print("Введите размер матрицы")
//    val n = readLine()!!.toInt()
//
//    for (i in 1 .. n ){
//        string.add(i)}
//    for (j in 1 .. n)
//        matrix.add(j)
//    print (matrix)
// }
